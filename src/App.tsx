import { cn } from "./utils/cn";

function App() {
  return (
    <div
      className={cn(
        "w-[400px] h-[400px] font-bold flex items-center justify-center"
      )}
    >
      Hello world
    </div>
  );
}

export default App;
